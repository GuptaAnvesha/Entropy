# Copy to functions/.env for local emulator runs (firebase emulators:start).
# For deployed functions, store the key in Secret Manager instead:
#   firebase functions:secrets:set GEMINI_API_KEY
# The functions bind the secret via their `secrets` option; never commit a real key.
GEMINI_API_KEY=your-gemini-api-key-here
